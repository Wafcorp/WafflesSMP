### Server rules by WafflesAreBetter

## Commands to run every tick:

execute as @a[tag=!seen_rules] run function serverrules:rules
execute as @a[tag=seen_rules,tag=!acceptedrules] run function serverrules:messages